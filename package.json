{
  "name": "nuxt-gcloud",
  "version": "1.0.0",
  "description": "Nuxt gcloud multiple services",
  "author": "Zalo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:default\" \"npm run dev:service1\" \"npm run dev:service2\"",
    "dev:default": "ENV=dev nuxt --config-file services/default/nuxt.local.js -p=3000",
    "dev:service1": "ENV=dev nuxt --config-file services/service1/nuxt.local.js -p=3001",
    "dev:service2": "ENV=dev nuxt --config-file services/service2/nuxt.local.js -p=3002",
    "dev:api": "ENV=dev PORT=5000 nodemon services/api/server.js",
    "build:default": "ENV=build nuxt build --config-file services/default/nuxt.local.js",
    "build:service1": "ENV=build nuxt build --config-file services/service1/nuxt.local.js",
    "build:service2": "ENV=build nuxt build --config-file services/service2/nuxt.local.js",
    "deploy": "npm run deploy:default && npm run deploy:service1 && npm run deploy:service2 && npm run deploy:api && npm run deploy:dispatch",
    "deploy:default": "npm run build:default && cpx nuxt.global.js services/default && cpx package.json services/default && gcloud app deploy --project nuxt-services services/default/app.yaml --quiet && rm services/default/package.json && rm services/default/nuxt.global.js",
    "deploy:service1": "npm run build:service1 && cpx package.json services/service1 && gcloud app deploy --project nuxt-services services/service1/app.yaml --quiet && rm services/service1/package.json",
    "deploy:service2": "npm run build:service2 && cpx package.json services/service2 && gcloud app deploy --project nuxt-services services/service2/app.yaml --quiet && rm services/service2/package.json",
    "deploy:api": "gcloud app deploy --project nuxt-services services/api/app.yaml --quiet",
    "deploy:dispatch": "gcloud app deploy services/dispatch.yaml --project nuxt-services --quiet",
    "start": "nuxt start",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore ."
  },
  "dependencies": {
    "axios": "^0.19.2",
    "cors": "^2.8.5",
    "deepmerge": "^4.2.2",
    "express": "^4.17.1",
    "nodemon": "^2.0.3",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "concurrently": "^5.2.0"
  }
}
